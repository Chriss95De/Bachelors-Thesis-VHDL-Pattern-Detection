library ieee;
use ieee.std_logic_1164.ALL;
use ieee.numeric_std.ALL;


entity COMPARATOR_x_pos_test is
generic(
	THRESHOLD : POSITIVE := 200;	-- threshold value for indicate an object
);
PORT  
(
	clk				: in std_logic;
	reset				: in std_logic;
	data_in			: in std_logic_vector (8 downto 0);
	data_in_valid	: in std_logic;
	x_pos_in			: in unsigned (9 downto 0);

);
end entity COMPARATOR_x_pos_test;


architecture a of COMPARATOR_x_pos_test is
begin
process (reset, clk) is
	if reset = '1' then	-- reset all values

	elsif rising_edge(clk) then

	end if;
	
end process;

end a;